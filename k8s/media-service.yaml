apiVersion: apps/v1
kind: Deployment
metadata:
  name: media-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: media-service
  template:
    metadata:
      labels:
        app: media-service
    spec:
      imagePullSecrets:
      - name: ecr-pull-secret
      containers:
      - name: media-container
        image: $IMAGE_URL
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: media-config
        - secretRef:
            name: media-secret
---
apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: default
spec:
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: media-service
